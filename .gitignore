# nginx 渲染产物
.nginx.rendered.conf
.nginx.pid
.nginx.log

# reload-server 运行产物
.reload-server.log
.reload-server.pid

# 系统垃圾
.DS_Store
*.swp

# nginx 二进制(三个平台的 nginx 在 bin/,跨平台分发需要 commit 进 git,不在 gitignore 里)